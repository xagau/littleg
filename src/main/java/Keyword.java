public enum Keyword {
    IF,
    DECIMAL,
    BOOLEAN,
    CLASS,
    OBJECT,
    STRING,
    FOR,
    WHILE,
    BREAK,
    CONTINUE,
    RETURN,
    NULL,
    SWITCH,
    CASE,
    NEW,
    ELSE
}
